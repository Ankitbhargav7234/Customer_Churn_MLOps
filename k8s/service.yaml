apiVersion: v1
kind: Service
metadata:
  name: customer-churn-mlops-service
spec:
  type: NodePort
  selector:
    app: customer-churn-mlops
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
      nodePort: 30007